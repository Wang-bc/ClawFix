# 常见问题 FAQ

## Redis 连接失败

如果日志中出现 `Connection refused`、`timeout` 或 `NOAUTH`，优先检查：

1. Redis 实例是否正常监听
2. 连接串、密码和 db index 是否正确
3. 应用所在网络是否能访问 Redis 端口

## 配置中心未生效

如果应用启动后读取到默认值或空值，优先核对：

1. 环境变量是否被正确注入
2. 配置文件是否被打包进部署产物
3. 配置中心命名空间和版本是否正确

